STEP_SOLID_SERVICE_URL=http://127.0.0.1:8001/export-step-solid
VITE_STEP_SOLID_DIRECT_URL=https://novamotis-step-solid-service.onrender.com/export-step-solid

# Shared beta password for /profile-configurator (client-side gate, not real auth)
VITE_PROFILE_BETA_PASSWORD=

# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX) — only loaded after
# cookie-banner consent (see src/lib/google-analytics.ts). Leave empty to
# disable analytics entirely.
VITE_GA_MEASUREMENT_ID=

